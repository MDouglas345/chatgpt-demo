{
  "name": "chatgpt-demo-frontend",
  "version": "1.0.0",
  "description": "The IO of the operation. Gets input from the user, sends it off, and displays the response",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Michael Douglas",
  "license": "ISC",
  "dependencies": {
    "bootstrap": "^5.2.3",
    "jquery": "^3.6.2"
  }
}
